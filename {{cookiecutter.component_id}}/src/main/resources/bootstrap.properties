# spring.cloud.config.enabled=false stops spring from trying to contact the config server (which is currently not configured)
# Without this property, an error will be logged on application startup that has no influence on the service's functionality.
spring.cloud.config.enabled=false